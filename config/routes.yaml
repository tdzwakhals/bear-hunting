controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

security_domain:
    resource: 'routes/domains/security.yaml'

hunt_domain:
    resource: 'routes/domains/hunt.yaml'

bear_domain:
    resource: 'routes/domains/bear.yaml'

user_domain:
    resource: 'routes/domains/user.yaml'